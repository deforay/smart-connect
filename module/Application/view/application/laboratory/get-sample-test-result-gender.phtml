<?php
//Zend\Debug\Debug::dump($result['lab']);die;
?>
<div id="facilitiesTestedGender"></div>
<script>
       $(function () {
        $('#facilitiesTestedGender').highcharts({
       chart: {
            type: 'column'
        },
        title: {
            text: ''
        },
        xAxis: {
            categories: [<?php
            if(isset($result['date']) && count($result['date'])>0){
            foreach($result['date'] as $date){
                echo "'".$date."',";
            }
            }
            ?>]
        },
        yAxis: {
            allowDecimals: false,
            min: 0,
            title: {
                text: ''
            }
        },
        tooltip: {
            formatter: function () {
                return '<b>' + this.x + '</b><br/>' +
                    this.series.name + ': ' + this.y + '<br/>' +
                    'Total: ' + this.point.stackTotal;
            }
        },
        plotOptions: {
            column: {
                stacking: 'normal',
                pointPadding: 0.2,
                borderWidth: 0,
                cursor: 'pointer',
                point: {
                    events: {
                        click: function () {
                            window.location.href='/labs/samples-tested';
                        }
                    }
                }
            }
        },
        series: [<?php
        $gender = array('M','F','');
        for($i=0;$i<3;$i++){
              $specific = $gender[$i];
              if($gender[$i]=='')
              {
                     $specific = 'Not Specified';
              }
              foreach($result[$gender[$i]]['sampleName'] as $key=>$condition){
              ?>
              {
              name:'<?php echo $key."(".$specific.")";?>',
              data:[<?php
                  foreach($condition as $count){
                      echo $count.",";
                  }
              ?>],
              stack:'<?php echo $gender[$i];?>',
              },
              <?php
              }
              ?>
              
              <?php
        }
            ?>
        ],
        colors: ['#ff1900','#333333','#60d18f',],
    });
    });
</script>