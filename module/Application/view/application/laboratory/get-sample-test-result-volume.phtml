<?php
//\Zend\Debug\Debug::dump($result);die;
?>
<div id="facilitiesTestedVsSuppressed"></div>

<script>
       $(function () {
        $('#facilitiesTestedVsSuppressed').highcharts({
        chart: {
            type: 'column'
        },
        title: {
            text: ''
        },
        xAxis: {
            categories: [<?php
            foreach($result['lab'] as $lab){
                echo "'".$lab['facility_name']."',";
            }
            ?>]
        },
        yAxis: {
            allowDecimals: false,
            min: 0,
            title: {
                text: ''
            }
        },
        tooltip: {
            formatter: function () {
                return '<b>' + this.x + '</b><br/>' +
                    this.series.name + ': ' + this.y + '<br/>' +
                    'Total: ' + this.point.stackTotal;
            }
        },
        plotOptions: {
            column: {
                stacking: 'normal',
                pointPadding: 0.2,
                borderWidth: 0,
                cursor: 'pointer',
                point: {
                    events: {
                        click: function () {
                            window.location.href='/labs/samples-tested';
                        }
                    }
                }
            }
        },
        series: [
            <?php
            $name = 'female';
            foreach($result['lab'] as $lab){
              if(isset($result['result'][$lab['sample_name']])){
                     if($name == 'female' ){
                         $name = 'male'   ;
                     }else{
                         $name = 'female';   
                     }
                    foreach($result['result'][$lab['sample_name']] as $key=>$condition){
                    ?>
                    {
                    name:'<?php echo $lab['sample_name']." - ".$key;?>',
                    data:[<?php
                        foreach($condition as $count){
                            echo $count.",";
                        }
                    ?>],
                    stack:'<?php echo $name;?>'
                    },
                    <?php
                    $i++;
                    }
              }
            }
            ?>
        ],
    });
    });
</script>